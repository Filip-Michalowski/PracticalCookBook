<UserControl x:Class="PracticalCookBook.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PracticalCookBook.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid
        Margin="0 12 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
            HorizontalAlignment="Center">
            Tekst z głównego widoku
        </TextBlock>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Orientation="Vertical">
            <Button
                Width="250"
                Height="20"
                Command="{Binding GoToNewRecipe}">
                Nowy przepis
            </Button>
            <Button
                Width="250"
                Height="20"
                Command="{Binding GoToExampleRecipe}">
                Pokaż mi przykładowy przepis. (id:1)
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
