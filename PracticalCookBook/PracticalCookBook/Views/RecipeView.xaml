<UserControl x:Class="PracticalCookBook.Views.RecipeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PracticalCookBook.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0">
                <Run>Przykładowy nagłówek przepisu. id:</Run>
                <Run Text="{Binding RecipeId, Mode = OneWay}" />
                <!--
                <MultiBinding StringFormat="Przykładowy nagłówek przepisu. id:{0}">
                    <Binding Path="RecipeId" />
                </MultiBinding>
                -->
        </TextBlock>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0">
            <Button Command="{Binding GoToMainMenu}">Wróć do menu głównego</Button>
        </StackPanel>
    </Grid>
</UserControl>
